export enum PrivacyStatus {
  PRIVATE = 'private',
  UNLISTED = 'unlisted',
  PUBLIC = 'public',
}

export enum UploadStatus {
  DELETED = 'deleted',
  FAILED = 'failed',
  PROCESSED = 'processed',
  REJECTED = 'rejected',
  UPLOADED = 'uploaded',
}
